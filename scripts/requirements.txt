# KAN-SDM: Python dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# KAN
# Install from source: pip install git+https://github.com/KindXiaoming/pykan.git
# (pykan is not on PyPI; see https://github.com/KindXiaoming/pykan)

# Geospatial
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0

# XGBoost
xgboost>=2.0.0

# Plotting
matplotlib>=3.7.0

# Utilities
joblib>=1.3.0

# R dependencies (install in R separately):
# install.packages(c("maxnet", "blockCV", "dismo", "pROC", "terra", "sf"))
